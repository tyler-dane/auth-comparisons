{
   "name": "auth-comparisons",
   "version": "1.0.0",
   "main": "index.js",
   "author": "Tyler Dane",
   "license": "MIT",
   "private": true,
   "workspaces": [
      "frontend",
      "backend"
   ],
   "scripts": {
      "start": "node backend/app.js",
      "dev": "concurrently \"node backend/app.js\" \"cd frontend && react-scripts start\"",
      "build": "yarn --cwd frontend build"
   },
   "devDependencies": {
      "concurrently": "^8.2.0"
   }
}
